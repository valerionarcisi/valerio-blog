---
import { Icon } from "astro-icon/components";
import { timeSafeEqual } from "~/lib/auth";
import { env } from "~/lib/env";
import AdminNav from "~/components/AdminNav.astro";
import "~/styles/foglio.css";
import "~/styles/admin-foglio.css";

export const prerender = false;

const token = Astro.url.searchParams.get("token") ?? "";
const isAuthorized = token.length > 0 && timeSafeEqual(token, env("ADMIN_TOKEN"));

if (!isAuthorized) {
  return new Response("Unauthorized", { status: 401 });
}

const tickets = [
  {
    name: "Valerio Narcisi",
    type: "Intero",
    price: "18,00 €",
    code: "SPCO3YZV3ENGV7ZN",
    icon: "ph:user",
  },
  {
    name: "Adriana Pastore",
    type: "Intero",
    price: "18,00 €",
    code: "SPCO7EB7VZGXA8GE",
    icon: "ph:user",
  },
  {
    name: "Bianca Narcisi",
    type: "Gratuito · Under 18",
    price: "0,00 €",
    code: "SPCOKPV782WAD36B",
    icon: "ph:baby",
  },
  {
    name: "Lea Narcisi",
    type: "Gratuito · Under 18",
    price: "0,00 €",
    code: "SPCOAGE4JA3QDD6K",
    icon: "ph:baby",
  },
];

const rules = [
  {
    icon: "ph:clock-countdown",
    title: "Il superpotere 24h",
    text: "L'ingresso al Colosseo e' fissato alle 16:45 del Sabato. Per Foro Romano/Palatino/Fori Imperiali hai un singolo ingresso entro 24h antecedenti o successive. Spezzi la visita per non stancare le bimbe.",
  },
  {
    icon: "ph:eye",
    title: "Cosa vedete dentro",
    text: "Colosseo: primo e secondo ordine (vista panoramica classica). Esclusi arena, sotterranei e attico. Fuori: Foro Romano, Palatino, Fori Imperiali + mostre e musei.",
  },
  {
    icon: "ph:identification-card",
    title: "Documenti tassativi",
    text: "Documento d'identita' obbligatorio all'entrata del Colosseo e del Foro Romano/Palatino. Vitale per dimostrare l'eta' di Bianca e Lea per i biglietti gratuiti.",
  },
  {
    icon: "ph:backpack",
    title: "Zaini in auto",
    text: "Vietati zaini voluminosi, valigie e trolley. Lasciate tutto nel bagagliaio della Jogger.",
  },
  {
    icon: "ph:timer",
    title: "Puntualita' e permanenza",
    text: "Presentarsi all'ingresso del Colosseo alle 16:30 (15 min prima). All'interno, 75 minuti di visita. Percorso a senso unico.",
  },
  {
    icon: "ph:sneaker",
    title: "Comfort",
    text: "Scarpe comode, indispensabili per sampietrini e rovine. I bagni dentro al Colosseo si trovano solo all'inizio del percorso.",
  },
];

function mapsSearch(query: string): string {
  return `https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(query)}`;
}

function mapsDirections(from: string, to: string): string {
  return `https://www.google.com/maps/dir/?api=1&origin=${encodeURIComponent(from)}&destination=${encodeURIComponent(to)}`;
}

const timeline = [
  {
    time: "08:00",
    title: "Partenza da Porto Sant'Elpidio",
    desc: "Viaggio con la Jogger Full Hybrid.",
    icon: "ph:car",
    places: [
      { label: "Rotta Porto Sant'Elpidio → Roma", url: mapsDirections("Porto Sant'Elpidio, FM", "San Saba, Roma") },
    ],
  },
  {
    time: "11:00",
    title: "Arrivo e parcheggio",
    desc: "Zona San Saba / Aventino (strisce blu gratis). Lasciare in auto borse ingombranti. Portare solo passeggino leggero per Lea, acqua e documenti.",
    icon: "ph:map-pin",
    places: [
      { label: "Parcheggio San Saba", url: mapsSearch("Parcheggio San Saba Roma strisce blu") },
      { label: "Aventino", url: mapsSearch("Aventino Roma parcheggio") },
    ],
  },
  {
    time: "11:45",
    title: "Foro Romano e Palatino",
    desc: "Sfruttiamo la regola 24h antecedenti: passeggiata tranquilla tra le rovine prima che faccia troppo caldo. Le bimbe avranno spazio per camminare.",
    icon: "ph:tree",
    places: [
      { label: "Foro Romano", url: mapsSearch("Foro Romano Roma") },
      { label: "Palatino", url: mapsSearch("Palatino Roma ingresso Via di San Gregorio") },
    ],
  },
  {
    time: "13:15",
    title: "Pranzo di ricarica",
    desc: "Uscita dall'area archeologica verso il Rione Monti. Pizza al taglio o pasta.",
    icon: "ph:pizza",
    places: [
      { label: "Rione Monti", url: mapsSearch("Rione Monti Roma ristoranti") },
      { label: "La Carbonara (Monti)", url: mapsSearch("La Carbonara Monti Roma") },
    ],
  },
  {
    time: "14:30",
    title: "Pausa relax (o Egitto)",
    desc: "Due ore buche. Riposo per Lea sul passeggino al parco del Colle Oppio. Opzione: mostra Egizia alle Scuderie del Quirinale (valutare la stanchezza).",
    icon: "ph:park",
    places: [
      { label: "Parco del Colle Oppio", url: mapsSearch("Parco del Colle Oppio Roma") },
      { label: "Scuderie del Quirinale", url: mapsSearch("Scuderie del Quirinale Roma") },
    ],
  },
  {
    time: "16:30",
    title: "Appuntamento con la storia",
    desc: "Presentarsi all'ingresso del Colosseo con i documenti pronti.",
    icon: "ph:identification-badge",
    places: [
      { label: "Ingresso Colosseo · Piazza del Colosseo", url: mapsSearch("Colosseo ingresso Piazza del Colosseo Roma") },
    ],
  },
  {
    time: "16:45",
    title: "Ingresso al Colosseo",
    desc: "Primo e secondo ordine. 75 minuti per godersi il tramonto tra le arcate. Bagni all'inizio del percorso.",
    icon: "ph:buildings",
    places: [
      { label: "Colosseo", url: mapsSearch("Colosseo Roma") },
    ],
  },
  {
    time: "18:00",
    title: "Uscita, gelato e rientro",
    desc: "Ritorno verso l'Aventino per riprendere la Jogger, gelato lungo la strada per premiare le piccole esploratrici.",
    icon: "ph:ice-cream",
    places: [
      { label: "Gelateria Fatamorgana Monti", url: mapsSearch("Fatamorgana gelateria Monti Roma") },
      { label: "Rotta Roma → Porto Sant'Elpidio", url: mapsDirections("Colosseo, Roma", "Porto Sant'Elpidio, FM") },
    ],
  },
];

const entrances = [
  { label: "Piazza del Colosseo > Valadier", url: mapsSearch("Piazza del Colosseo Valadier Roma") },
  { label: "Via Sacra > Arco di Tito", url: mapsSearch("Arco di Tito Via Sacra Roma") },
  { label: "Largo della Salara Vecchia", url: mapsSearch("Largo della Salara Vecchia Roma") },
  { label: "Via di San Pietro in Carcere", url: mapsSearch("Via di San Pietro in Carcere Roma") },
  { label: "Piazza Venezia > Colonna Traiana", url: mapsSearch("Colonna Traiana Piazza Venezia Roma") },
  { label: "(Via del) Foro Romano", url: mapsSearch("Via del Foro Romano Roma") },
  { label: "Via di San Gregorio", url: mapsSearch("Via di San Gregorio Roma Colosseo") },
];
---

<!doctype html>
<html lang="it" class="foglio">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex, nofollow" />
    <meta name="referrer" content="no-referrer" />
    <title>Roma · 11 Aprile 2026</title>
    <style is:global>
      .foglio body.admin .container { max-width: 1100px; margin: 0 auto; }

      .foglio body.admin .page-header { margin-bottom: 2rem; padding-bottom: 1.5rem; border-bottom: 1px solid var(--foglio-rule); }
      .foglio body.admin .page-kicker { color: var(--foglio-ink-soft); font-family: var(--foglio-mono); font-size: var(--foglio-fs-mono); text-transform: uppercase; letter-spacing: 0.14em; margin-bottom: 0.5rem; }
      .foglio body.admin .page-header h1 { font-family: var(--foglio-serif); color: var(--foglio-ink); font-size: var(--foglio-fs-4xl); font-weight: 500; letter-spacing: -0.015em; line-height: 1.05; margin-bottom: 0.5rem; }
      .foglio body.admin .page-subtitle { color: var(--foglio-ink-soft); font-family: var(--foglio-serif); font-style: italic; font-size: var(--foglio-fs-sm); display: flex; align-items: center; gap: 0.5rem; flex-wrap: wrap; }
      .foglio body.admin .page-subtitle .icon-inline { width: 1rem; height: 1rem; color: var(--foglio-accent); }
      .foglio body.admin .booking-ref { font-family: var(--foglio-mono); font-size: var(--foglio-fs-mono); color: var(--foglio-accent); text-transform: uppercase; letter-spacing: 0.1em; font-style: normal; }

      .foglio body.admin .card-title { font-family: var(--foglio-mono); color: var(--foglio-ink-soft); font-size: var(--foglio-fs-mono); font-weight: 600; text-transform: uppercase; letter-spacing: 0.14em; margin-bottom: 1.2rem; display: flex; align-items: center; gap: 0.55rem; padding-bottom: 0.6rem; border-bottom: 1px solid var(--foglio-rule); }
      .foglio body.admin .card-title-icon { width: 1.15rem; height: 1.15rem; color: var(--foglio-accent); }

      .foglio body.admin .tickets-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(240px, 1fr)); gap: 1rem; }
      .foglio body.admin .ticket { background: var(--foglio-paper-2); border: 1px solid var(--foglio-rule); border-radius: var(--foglio-radius-md); padding: 1.15rem; position: relative; overflow: hidden; }
      .foglio body.admin .ticket::before { content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px; background: var(--foglio-accent); }
      .foglio body.admin .ticket-head { display: flex; align-items: center; gap: 0.6rem; margin-bottom: 0.85rem; }
      .foglio body.admin .ticket-avatar { width: 2rem; height: 2rem; border-radius: 50%; background: var(--foglio-paper); border: 1px solid var(--foglio-accent); display: flex; align-items: center; justify-content: center; color: var(--foglio-accent); flex-shrink: 0; }
      .foglio body.admin .ticket-avatar svg { width: 1.1rem; height: 1.1rem; }
      .foglio body.admin .ticket-name { font-family: var(--foglio-serif); font-weight: 500; font-size: var(--foglio-fs-md); color: var(--foglio-ink); }
      .foglio body.admin .ticket-type { color: var(--foglio-ink-soft); font-size: var(--foglio-fs-mono); margin-bottom: 0.5rem; font-family: var(--foglio-mono); text-transform: uppercase; letter-spacing: 0.1em; }
      .foglio body.admin .ticket-price { font-family: var(--foglio-mono); color: var(--foglio-accent); font-weight: 700; font-size: var(--foglio-fs-md); margin-bottom: 0.85rem; font-variant-numeric: tabular-nums; }
      .foglio body.admin .ticket-price.free { color: var(--foglio-ink-soft); }
      .foglio body.admin .ticket-code { color: var(--foglio-ink-mute); font-family: var(--foglio-mono); font-size: 0.68rem; letter-spacing: 0.04em; padding-top: 0.7rem; border-top: 1px dashed var(--foglio-rule); word-break: break-all; }

      .foglio body.admin .rules-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 1rem; }
      .foglio body.admin .rule { background: var(--foglio-paper-2); border: 1px solid var(--foglio-rule); border-radius: var(--foglio-radius-md); padding: 1.1rem; }
      .foglio body.admin .rule-head { display: flex; align-items: center; gap: 0.55rem; margin-bottom: 0.55rem; }
      .foglio body.admin .rule-icon { width: 1.15rem; height: 1.15rem; color: var(--foglio-accent); flex-shrink: 0; }
      .foglio body.admin .rule-title { font-family: var(--foglio-serif); font-size: var(--foglio-fs-md); font-weight: 500; color: var(--foglio-ink); }
      .foglio body.admin .rule-text { font-family: var(--foglio-serif); font-size: var(--foglio-fs-sm); color: var(--foglio-ink-soft); line-height: 1.6; }

      .foglio body.admin .timeline { position: relative; padding-left: 2.5rem; }
      .foglio body.admin .timeline::before { content: ''; position: absolute; left: 0.95rem; top: 0.5rem; bottom: 0.5rem; width: 1px; background: var(--foglio-rule-strong); }
      .foglio body.admin .step { position: relative; padding-bottom: 1.6rem; }
      .foglio body.admin .step:last-child { padding-bottom: 0; }
      .foglio body.admin .step-dot { position: absolute; left: -2.5rem; top: 0.15rem; width: 2rem; height: 2rem; border-radius: 50%; background: var(--foglio-paper); border: 1px solid var(--foglio-accent); display: flex; align-items: center; justify-content: center; color: var(--foglio-accent); }
      .foglio body.admin .step-dot svg { width: 1rem; height: 1rem; }
      .foglio body.admin .step-time { font-family: var(--foglio-mono); font-size: var(--foglio-fs-mono); color: var(--foglio-accent); font-weight: 700; letter-spacing: 0.1em; }
      .foglio body.admin .step-title { font-family: var(--foglio-serif); font-size: var(--foglio-fs-md); font-weight: 500; color: var(--foglio-ink); margin: 0.1rem 0 0.35rem; }
      .foglio body.admin .step-desc { font-family: var(--foglio-serif); font-size: var(--foglio-fs-sm); color: var(--foglio-ink-soft); max-width: 700px; line-height: 1.6; }

      .foglio body.admin .entrance-list { list-style: none; display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 0.4rem 1rem; counter-reset: item; }
      .foglio body.admin .entrance-list li { font-family: var(--foglio-serif); font-size: var(--foglio-fs-sm); color: var(--foglio-ink-soft); padding: 0.4rem 0; display: flex; align-items: center; gap: 0.5rem; border-bottom: 1px solid var(--foglio-rule); }
      .foglio body.admin .entrance-list li::before { content: counter(item); counter-increment: item; width: 1.4rem; height: 1.4rem; background: var(--foglio-accent); color: var(--foglio-paper); border-radius: 50%; display: inline-flex; align-items: center; justify-content: center; font-family: var(--foglio-mono); font-size: 0.7rem; font-weight: 700; flex-shrink: 0; }
      .foglio body.admin .entrance-link { color: inherit; text-decoration: none; display: inline-flex; align-items: center; gap: 0.35rem; transition: color 0.15s; }
      .foglio body.admin .entrance-link:hover { color: var(--foglio-accent); }
      .foglio body.admin .entrance-link svg { color: var(--foglio-ink-mute); transition: color 0.15s; }
      .foglio body.admin .entrance-link:hover svg { color: var(--foglio-accent); }

      .foglio body.admin .step-places { display: flex; flex-wrap: wrap; gap: 0.4rem; margin-top: 0.65rem; }
      .foglio body.admin .map-link { display: inline-flex; align-items: center; gap: 0.35rem; padding: 0.3rem 0.7rem; border: 1px solid var(--foglio-rule); border-radius: var(--foglio-radius-sm); background: var(--foglio-paper-2); color: var(--foglio-ink); font-family: var(--foglio-mono); font-size: var(--foglio-fs-mono); text-transform: uppercase; letter-spacing: 0.08em; text-decoration: none; transition: all 0.15s; white-space: nowrap; }
      .foglio body.admin .map-link:hover { border-color: var(--foglio-accent); color: var(--foglio-accent); }
      .foglio body.admin .map-link svg { color: inherit; }

      .foglio body.admin .included-wrap { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
      .foglio body.admin .included-col h3 { font-family: var(--foglio-mono); color: var(--foglio-ink-soft); font-size: var(--foglio-fs-mono); text-transform: uppercase; letter-spacing: 0.14em; margin-bottom: 0.6rem; font-weight: 700; }
      .foglio body.admin .included-col.included h3 { color: #2e8b57; }
      .foglio body.admin .included-col.excluded h3 { color: #c0392b; }
      .foglio body.admin .included-col ul { list-style: none; }
      .foglio body.admin .included-col li { font-family: var(--foglio-serif); font-size: var(--foglio-fs-sm); color: var(--foglio-ink-soft); padding: 0.35rem 0; display: flex; align-items: center; gap: 0.5rem; border-bottom: 1px solid var(--foglio-rule); }
      .foglio body.admin .included-col li svg { width: 0.95rem; height: 0.95rem; flex-shrink: 0; }
      .foglio body.admin .included-col.included li svg { color: #2e8b57; }
      .foglio body.admin .included-col.excluded li svg { color: #c0392b; }

      @media (max-width: 720px) {
        .foglio body.admin .card { padding: 1.15rem; }
        .foglio body.admin .timeline { padding-left: 2rem; }
        .foglio body.admin .step-dot { left: -2rem; width: 1.7rem; height: 1.7rem; }
        .foglio body.admin .timeline::before { left: 0.8rem; }
        .foglio body.admin .included-wrap { grid-template-columns: 1fr; }
      }
    </style>
  </head>
  <body class="admin">
    <AdminNav currentPage="roma" token={token} />
    <div class="container">
      <header class="page-header">
        <div class="page-kicker">Trip · Famiglia Narcisi</div>
        <h1><em class="acc">Roma.</em> Sabato 11 Aprile 2026</h1>
        <p class="page-subtitle">
          <Icon name="ph:ticket" class="icon-inline" size={16} is:inline />
          <span>Colosseo · Foro Romano · Palatino 24H</span>
          <span>·</span>
          <span class="booking-ref">Prenotazione OCO3783643</span>
        </p>
      </header>

      <section class="card">
        <h2 class="card-title">
          <Icon name="ph:ticket" class="card-title-icon" size={18} is:inline />
          Biglietti · 4 persone
        </h2>
        <div class="tickets-grid">
          {tickets.map((t) => (
            <div class="ticket">
              <div class="ticket-head">
                <div class="ticket-avatar">
                  <Icon name={t.icon} size={18} is:inline />
                </div>
                <div class="ticket-name">{t.name}</div>
              </div>
              <div class="ticket-type">{t.type}</div>
              <div class={`ticket-price ${t.price === "0,00 €" ? "free" : ""}`}>{t.price}</div>
              <div class="ticket-code">{t.code}</div>
            </div>
          ))}
        </div>
      </section>

      <section class="card">
        <h2 class="card-title">
          <Icon name="ph:check-circle" class="card-title-icon" size={18} is:inline />
          Cosa include il biglietto
        </h2>
        <div class="included-wrap">
          <div class="included-col included">
            <h3>Incluso</h3>
            <ul>
              <li><Icon name="ph:check" size={16} is:inline />Colosseo · primo e secondo ordine</li>
              <li><Icon name="ph:check" size={16} is:inline />Foro Romano</li>
              <li><Icon name="ph:check" size={16} is:inline />Palatino</li>
              <li><Icon name="ph:check" size={16} is:inline />Fori Imperiali</li>
              <li><Icon name="ph:check" size={16} is:inline />Mostre e musei del circuito</li>
            </ul>
          </div>
          <div class="included-col excluded">
            <h3>Escluso</h3>
            <ul>
              <li><Icon name="ph:x" size={16} is:inline />Colosseo · arena</li>
              <li><Icon name="ph:x" size={16} is:inline />Colosseo · sotterranei</li>
              <li><Icon name="ph:x" size={16} is:inline />Colosseo · attico</li>
              <li><Icon name="ph:x" size={16} is:inline />Foro Romano/Palatino · siti super</li>
              <li><Icon name="ph:x" size={16} is:inline />Domus Area</li>
            </ul>
          </div>
        </div>
      </section>

      <section class="card">
        <h2 class="card-title">
          <Icon name="ph:warning-circle" class="card-title-icon" size={18} is:inline />
          Regole d'oro
        </h2>
        <div class="rules-grid">
          {rules.map((r) => (
            <div class="rule">
              <div class="rule-head">
                <span class="rule-icon"><Icon name={r.icon} size={18} is:inline /></span>
                <span class="rule-title">{r.title}</span>
              </div>
              <p class="rule-text">{r.text}</p>
            </div>
          ))}
        </div>
      </section>

      <section class="card">
        <h2 class="card-title">
          <Icon name="ph:path" class="card-title-icon" size={18} is:inline />
          Itinerario · Sabato
        </h2>
        <div class="timeline">
          {timeline.map((step) => (
            <div class="step">
              <div class="step-dot">
                <Icon name={step.icon} is:inline />
              </div>
              <div class="step-time">{step.time}</div>
              <div class="step-title">{step.title}</div>
              <div class="step-desc">{step.desc}</div>
              {step.places && step.places.length > 0 && (
                <div class="step-places">
                  {step.places.map((p) => (
                    <a class="map-link" href={p.url} target="_blank" rel="noopener noreferrer">
                      <Icon name="ph:map-trifold" size={14} is:inline />
                      <span>{p.label}</span>
                    </a>
                  ))}
                </div>
              )}
            </div>
          ))}
        </div>
      </section>

      <section class="card">
        <h2 class="card-title">
          <Icon name="ph:door-open" class="card-title-icon" size={18} is:inline />
          Entrate disponibili al Colosseo
        </h2>
        <ul class="entrance-list">
          {entrances.map((e) => (
            <li>
              <a class="entrance-link" href={e.url} target="_blank" rel="noopener noreferrer">
                {e.label}
                <Icon name="ph:arrow-square-out" size={13} is:inline />
              </a>
            </li>
          ))}
        </ul>
      </section>
    </div>
  </body>
</html>
